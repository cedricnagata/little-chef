# Core dependencies
pydantic>=2.0.0

# LangChain and LLM
langchain
langchain-openai
langchain-core
openai

# HTTP requests for Firecrawl
requests

# AWS Lambda runtime helpers
boto3

# Exclude problematic transitive dependency (no wheel for this version)
uuid-utils!=0.13.0
