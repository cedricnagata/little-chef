# Core dependencies
pydantic>=2.0.0
pydantic-settings

# LangChain and LLM
langchain
langgraph
langchain-openai
langchain-core
openai

# AWS SDK (includes Polly TTS, API Gateway Management)
boto3

# Exclude problematic transitive dependency (no wheel for this version)
uuid-utils!=0.13.0

